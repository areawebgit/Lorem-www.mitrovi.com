 $.each($('.searchSelect option'), function(key, optionElement){var curText=$(optionElement).text();$(this).attr('title', curText);var lengthToShortenTo=Math.round(parseInt($(this).parent('select').css('max-width'), 10) / 7.3);lengthToShortenTo=lengthToShortenTo-19;if (curText.length > lengthToShortenTo){$(this).text(curText.substring(0,(lengthToShortenTo), curText.length) + '... ');} });$('.shortenedSelect').change(function(){$(this).attr('title', ($(this).find('option:eq('+$(this).get(0).selectedIndex +')').attr('title')));});function jquery_ucwords(str,force){str=force ? str.toLowerCase() :str;return str.replace(/(\b)([a-zA-Z])/g, function(firstLetter){return firstLetter.toUpperCase();});}